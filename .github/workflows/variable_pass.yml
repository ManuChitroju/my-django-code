name: Pass Variable Between Jobs

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: echo "hello"
  #   outputs:
  #     result: ${{ steps.set-var.outputs.result }}
  #   steps:
  #     - name: Set output variable
  #       id: set-var
  #       run: echo "result=Hello World" >> $GITHUB_OUTPUT

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Print the output from build job
  #       run: echo "Output from build: ${{ needs.build.outputs.result }}"
